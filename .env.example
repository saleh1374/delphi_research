# PostgreSQL (for Railway / production)
DATABASE_URL=postgresql://user:password@host:5432/delphi_db

# Or for local SQLite:
# DATABASE_URL=sqlite:///./delphi.db

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=*

# Server port
PORT=8000